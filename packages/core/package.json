{
  "name": "@cursor-logs/core",
  "private": true,
  "version": "0.0.0",
  "description": "Path resolution, SQLite read, schema detection, and chat export for cursor-logs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
